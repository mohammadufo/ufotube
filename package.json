{
  "name": "ufotube",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev --prefix api\"  \"npm run dev --prefix client\" ",
    "build": "cd api && npm install && npm run build  && cd ../client && npm install && npm run build",
    "start": "TG_ALLOW_MIXED=ALLOW  node api/build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
